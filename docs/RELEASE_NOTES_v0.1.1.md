# Release Notes v0.1.1

- Report Viewer UX polish: Details are authoritative; table stays a compact overview.
- Sorting and global filter, with a clear "No results" state.
- Copy JSON in Details (clipboard + fallback).
- Invalid JSON banner; last valid report remains visible.
- Truncation + tooltips, sticky table header, numeric alignment, scrollable Details.
- Docs updated with contract notes and viewer limitations.
- No core or CLI changes.
